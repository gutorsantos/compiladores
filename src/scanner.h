// #ifndef SCANNER_H
// #define SCANNER_H

// #include <stdlib.h>
// #include <string.h>

// typedef struct str {
//     char* string;
//     struct str* next;
// } str;

// extern str* str_list;
// static inline char* insert_yyval(char* text);
// static inline void clear_yyval_list(str* ptr);

// #endif
